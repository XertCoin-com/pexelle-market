name: ⚡ Performance / Regression Report
description: Report slowdowns, regressions, high latency, memory/CPU spikes, or throughput drops
title: "[PERF] "
labels: ["performance", "needs-triage"]
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        ⚠️ **Read before submitting**
        
        This form is for **measurable performance issues**:
        - Latency, slow UI, API timeouts
        - CPU / memory spikes
        - Throughput drops
        - Regression after a known change
        
        If you cannot provide **numbers** (before/after or at least current measurements), your report will be closed.

  - type: dropdown
    id: perf_type
    attributes:
      label: Performance Issue Type
      options:
        - Latency increase (API / backend)
        - UI slowness / jank
        - Timeouts / retries
        - CPU spike
        - Memory leak / growth over time
        - Bundle size / load time
        - Database / query slowdown
        - Other
    validations:
      required: true

  - type: dropdown
    id: area
    attributes:
      label: Area / Flow Affected
      options:
        - QR Auth / Account Linking
        - Evidence Upload / Verification
        - Skill Graph / Profile
        - Job Discovery / Search
        - Job Apply / Dynamic Resume
        - AI Roadmap Generation
        - Market Analytics / Insights
        - General / Cross-cutting
        - Other
    validations:
      required: true

  - type: textarea
    id: summary
    attributes:
      label: Summary
      description: One clear sentence describing what got slower.
      placeholder: "Example: AI roadmap generation takes 25–40s instead of ~5s for similar profiles."
    validations:
      required: true

  - type: textarea
    id: traces
    attributes:
      label: Evidence
      description: Attach traces, logs, screenshots, HAR files, profiler output, or console logs. No evidence = no triage.
      placeholder: |
        - Screenshots / screen recording:
        - Browser DevTools Performance profile:
        - Network HAR:
        - Server logs / request ids:
        - Metrics dashboard snapshots (if any):

  - type: dropdown
    id: impact
    attributes:
      label: Impact Level
      options:
        - 🟡 Low (annoying but usable)
        - 🟠 Medium (blocks some users / flows)
        - 🔴 High (core flow becomes painful)
        - 🚨 Critical (timeouts / unusable / data risk)
    validations:
      required: true

